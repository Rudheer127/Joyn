// Tailwind v4 uses CSS-based configuration in globals.css via @theme inline.
// This file is kept for reference only — it is not processed by Tailwind v4.
// All design tokens (colors, fonts, spacing, border-radius) are defined in
// app/globals.css using the @theme inline directive.

const config = {};
export default config;
